import type { Metadata } from "next";
import { Inter, Fraunces, JetBrains_Mono } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

const fraunces = Fraunces({
  variable: "--font-fraunces",
  subsets: ["latin"],
  display: "swap",
  axes: ["opsz", "SOFT", "WONK"],
});

const jetbrainsMono = JetBrains_Mono({
  variable: "--font-jetbrains-mono",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: {
    default: "Expaura — Restaurant Experience Platform",
    template: "%s · Expaura",
  },
  description:
    "QR menus, guest feedback, waiter performance, and analytics for Rwanda's best restaurants.",
  metadataBase: new URL("https://expaura.rw"),
  openGraph: {
    title: "Expaura",
    description: "Experience + Aura. The restaurant platform that makes every meal count.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${inter.variable} ${fraunces.variable} ${jetbrainsMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-ceramic text-ink">
        {children}
      </body>
    </html>
  );
}
